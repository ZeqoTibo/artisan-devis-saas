api_platform:
    title: API SASS
    version: 1.0.0
    enable_scalar: true
    swagger:
        # Conserve le JWT saisi dans « Authorize » après rechargement de /api/docs.
        persist_authorization: true
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
